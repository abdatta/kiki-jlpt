# Curated Library

This directory stores audio-backed conversations that have been added to the Library board.

- `sets/set-XX.json` contains curated conversation metadata grouped by vocabulary set.
- `audio/set-XX/` contains the copied audio files referenced by those JSON files.

These files are intended to be tracked in git so external apps can fetch the curated content through GitHub or static raw file URLs.
