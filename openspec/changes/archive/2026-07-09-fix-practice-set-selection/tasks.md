## 1. Studio Selection Behavior

- [x] 1.1 Update Studio generic Runs route selection to choose runs only from the active sidebar set.
- [x] 1.2 Keep the selected Studio set unchanged and show the empty run state when no run exists for that set.

## 2. Verification

- [x] 2.1 Add regression coverage for Studio run selection when the selected set has no matching run.
- [x] 2.2 Run the relevant unit tests and TypeScript/build checks for the Studio app.
